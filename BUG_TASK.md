## 题目元信息

- 来源：`self_built`
- 处理流程：`Codex Gold 修复`
- Bug 分类：`concurrency`
- 复现稳定性：`stable`
- 难度：`advanced`
- 目标 Bug 数量：`1`
- 上游 Issue：无（0-1 自建项目）

# BUG_TASK

- 来源（source）：`self_built`
- 题目（title）：优雅退出在排空队列前取消工作线程，丢弃已接受的缓冲事件
- Bug 分类（category）：并发（concurrency）/ 错误结果（wrong_result）

## 用户可见症状

突发写入后立即触发优雅退出，进程正常退出，且部分请求已经返回 `202 Accepted`
（表示事件已被接受）。但重启回放 WAL 后，发现这批已经返回 `202` 的事件中，
末尾若干条从未被持久化——它们既不在 WAL 里，也无法从索引恢复，造成数据丢失。

在压测或联调时，如果只在 shutdown 之前观察，事件看似都写入了；只有等到进程退出、
重启回放、或统计最终的持久化计数时，才会发现末尾一批事件凭空消失。这很容易被误判为
「客户端提前退出」或「网络丢包」，但实际上是服务端在已经确认 `202` 之后才丢掉了数据。

## 正确行为

任何已经返回 `202 Accepted`（即已被接受）的事件，在进程退出前都必须被排空并持久化；
重启回放 WAL 后，这些事件必须全部可恢复，不得丢失任何一条已确认事件。

## 稳定复现命令

```sh
go test -run TestGracefulShutdownDrainsAllAcceptedEvents -count=1 ./internal/ingest/
```

该测试模拟「突发写入 + 立即优雅退出」：向有界队列并发写入 40 个事件、确认全部被接受后
立即触发 Shutdown，随后断言持久化计数等于 40。当前实现会因丢弃缓冲事件而稳定失败。

## 验收标准

1. 正常路径：单个事件入队并 Shutdown 后，事件被持久化，重启回放可恢复。
2. 并发/失败路径：队列中存在多个缓冲事件时触发 Shutdown，所有已接受事件均被排空并
   持久化，不得丢弃任何一条。
3. 无回归：Shutdown 后工作线程全部退出、无 goroutine 泄漏，重复调用 Shutdown 幂等。
4. 仓库保持可构建：`go build ./...` 与 `go test -run '^$' -count=1 ./...` 通过，
   `go test -count=1 ./...` 中该聚焦测试稳定通过（不再失败）。
